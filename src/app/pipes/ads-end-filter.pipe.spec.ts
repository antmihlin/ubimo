import { AdsEndFilterPipe } from './ads-end-filter.pipe';

describe('AdsEndFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AdsEndFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
