import { AdsStartFilterPipe } from './ads-start-filter.pipe';

describe('AdsStartFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AdsStartFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
